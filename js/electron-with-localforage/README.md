# electron-with-localforage

Using `electron-quick-start` project as a base for a working example of using electron with localforage.

## To use

```bash
# Install dependencies
npm install
# Run the app
npm start
```